# ksajan.github.io

Personal portfolio + writing site built with Hugo (Sans theme, customized). Dark mode, inline search with tag filters on Writing, and an embedded resume.

Run locally:
```bash
hugo server -D
```
